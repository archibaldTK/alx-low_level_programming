#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: the parameters for the function
 * code by ArchibaldTK
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
