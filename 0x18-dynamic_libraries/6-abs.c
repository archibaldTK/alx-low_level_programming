#include "main.h"
/**
 * _abs - gives the absolute value on a number
 * @a: number that will be converted to its absolute value
 * Return:absolute value of a
 */
int _abs(int a)
{
	int i = (-1);

	if (a < 0)
		return (a * i);
	else
		return (a);
}
