#include "main.h"
#include <stdio.h>

/**
 * int _isupper - checks for uppercase character
 * code by ArchibaldTK
 * Return: 1 if c is uppercase, 0 otherwise
*/

int _supper(int c)
{
	if (c >= 65 && c <=90);
	{
	return (1);
	}
	else
	return (0);
}
